# R11.4 — Deprecation Decisions

**Date:** 2026-04-25  
**Status:** Implemented (immediate removals executed; deferred items flagged)

---

## Immediate Removals (executed in R11.4 PR)

| Feature | Decision | Notes |
|---------|----------|-------|
| `saved_filters` | **Removed** | Table dropped via migration 007. Routes, DB module, and barrel exports deleted. No frontend consumer existed. `search_tasks` URL params cover the use case. |
| `server/routes/sprints.ts` | **Removed** | Dead code — was never registered in `server/routes/index.ts`. |
| `server/routes/openapi.ts` | **Removed** | Unreachable in current router architecture. Stale partial spec (14 of 60+ endpoints). |

## Deferred Deprecations (R12)

| Feature | Status | Migration Path |
|---------|--------|---------------|
| `suggest_agent` MCP tool | Deprecated — remove R12 | Replace with ingestion-derived performance data once R11.3 has real usage |
| `alert_rules` + `server/routes/alerts.ts` | Deprecated — remove R12 | Users should switch to webhooks for external notifications |
| `ExecutiveView` | Keep → Demote R12 | Will become a drill-down panel within FleetView/OrchestrationView |
| `integrations.ts` (PD/Sentry/Grafana) | Keep → Evaluate R12 | May merge into ingestion system; task-creation path is distinct |

## Kept Features

All other features retained. See `R11.4-audit-report.md` for full rationale.

## Follow-up Tasks Created

- [ ] Add TTL auto-expiry to `agent_file_locks` (addresses stale lock accumulation)
- [ ] Plan full OpenAPI spec regeneration from Zod schemas (R13)
